HUB

    CCT -> CONNECT to ROOM => CCT[ROOM_NAME] => PORT
    CRT -> CREATE ROOM => CRT[ROOM_NAME]\\n[ROOM_PASSWORD]\\n[ROOM_PLAYERS] => PORT
    RMV -> DELETE ROOM => RMV[ROOM_NAME] => void
    LSR -> LIST ROOMS => LSR => List[room1, room2, ...]

CLIENT

    RCS -> ROOM CREATED with SUCESS => RCS[ROOM_NAME]
    AEX -> ALREADY EXIST => AEX[]
    CCT -> CONNECT to ROOM => CCT[ROOM_NAME]
    NEX -> NON EXISTENT => NEX[ROOM_NAME]
    WPD -> WRONG PASSWORD => WPD
    UCM -> UNKNOWN COMMAND => UCM[COMMAND]
    MOV -> MOVEMENT on turn => MOV
    ERR -> ERROR
    SUC -> SUCCESS
    AEN -> ANSWER ENVIDO CALL => AEN[envido/real_envido/falta_envido]
    ATC -> ANSWER TRUCO CALL => ATC[truco/retruco/vale_quatro]
    AFR -> ANSWER FLOR => AFR
    INF -> GAME INFO => INF[Dict with Game Infos]
    END -> GAME ENDED => END
    CCF -> CAN CALL FLOR => CCF
    RND -> ROUND ENDED => RND[Dict]
    TND -> TURN ENDED => TND